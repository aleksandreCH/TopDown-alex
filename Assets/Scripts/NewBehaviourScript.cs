using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Sum(10, 7, "ჯამი უდრის ");
    }

    // Update is called once per frame
    void Update()
    {

    }
    private int Sum(int a, int b)
    {
        int result = a + b;
        Debug.Log(jami udris, 17);


    }
    private void ShowMessage(string name, string lastname,)
    {
        string result = name + "" + lastname;
        Debug.Log(name + lastname = alexandre chogovadze);
    }
    private void ShowMessage(string name, string lastname, int name)
string result = name + "" + lastname + "" + name;
    Debug.Log(name + lastname + age = alexandre chogovadze 13);
}
